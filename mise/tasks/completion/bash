#!/usr/bin/env bash
#MISE description="Run the bash completion integration tests in containers"
#MISE depends=["completion:build-linux"]
set -euo pipefail

tests=()
for dockerfile in completion-integrations/tests/Dockerfile.*bash*; do
  tests+=("${dockerfile##*/Dockerfile.}")
done

exec completion-integrations/src/test-all.sh "${tests[@]}"
